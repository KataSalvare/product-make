# 工作流程指南

本文档整合原型/组件开发的全流程，作为各阶段规则的总入口与导航。项目中的对象必须先区分为「原型浏览工具」「前端原型」或「后台原型」，统一术语见根目录 `CONTEXT.md`。

## 🧭 核心顺序

```text
产品需求确认 -> 设计方案确认 -> 原型实现
```

| 阶段 | 继续前必须确认 | 参考文档 |
|------|----------------|----------|
| 产品需求 | 目标用户、核心任务、范围、功能清单、内容来源和验收重点 | `rules/requirements-alignment-guide.md` |
| 设计方案 | `DESIGN.md` 设计基底、信息架构、交互路径、关键组件取舍和视觉方向 | `rules/requirements-alignment-guide.md` |
| 原型实现 | 根据已确认的需求和设计方案实现原型 | `rules/prototype-development-guide.md` |

## 三类对象定义（必须先判断）

| 对象 | 是什么 | 代码位置 | 典型内容 |
|------|--------|----------|----------|
| 原型浏览工具 | 承载、切换、预览和评审原型的工作台 | `tools/` | 侧边栏、顶部栏、路由、批注、快捷键、Figma 复制 |
| 前端原型 | 面向终端用户的业务产品页面 | `src/prototypes/<name>/` | 登录、首页、个人中心、消费流程 |
| 后台原型 | 面向运营、管理或内部工作人员的业务页面 | `src/prototypes/<name>/` | 仪表盘、用户管理、订单管理、数据工作台 |

硬规则：前端原型和后台原型都是业务内容，都放在 `src/prototypes/`；后台原型不是工具层。只有服务于原型浏览工作台本身的功能才放在 `tools/`。

开始实现前，必须先写明：

```text
对象类型：原型浏览工具 / 前端原型 / 后台原型
内容位置：src/prototypes/<name>/（业务原型）或 tools/<directory>/（浏览工具）
路由分类：frontend / admin（仅业务原型需要）
```

## 文档事实边界

项目中的产品文档、页面规格和原型代码承担不同职责，不能互相替代：

- **PRD**：真实的产品目标、业务规则、数据交互语义、权限和验收标准，是产品行为的主要依据。
- **数据/流程/状态/权限文档**：补充真实数据对象、状态流转、角色边界和业务流程；存在时与 PRD 一起作为真实业务依据。
- **SPEC**：页面结构、视觉表现、页面状态、原型 mock 映射和原型/生产边界，不负责重新定义真实业务规则。
- **原型代码**：用于演示页面和交互，不得根据 mock 字段、固定延时、假成功或静态数据反推生产逻辑。

文档冲突时，先依据 PRD 及真实业务模型判断；SPEC 只覆盖页面表现；原型代码仅作为演示参考。无法判断的内容必须标记为“待确认”，不得自行补全。

## 原型内部组织（默认方案）

前端原型和后台原型都按同一套内部结构组织：

- `index.tsx`：原型入口和页面组装，只负责连接逻辑与展示 UI，不承载复杂业务状态。
- `*View.tsx`：展示 UI，只接收数据、状态和回调，不直接决定业务流程。
- `use*Logic.ts` 或原型目录内的 `hooks/`：交互逻辑、状态、校验、数据转换和事件处理，不写 JSX 和视觉样式。
- `src/prototypes/style.css`：多个原型共用的内容层样式、变量和动画；选择器必须使用 `prototype-` 命名空间。
- 原型目录内的 `style.css`：仅存放无法复用的单个原型专属样式，并说明原因。

新建或重构原型时，优先采用以上拆分；小型一次性交互可以留在入口附近，但不得让 `index.tsx` 同时承担整页 UI、复杂状态和全部事件流程。

## 额外产物

| 产物/场景 | 位置 | 参考文档 |
|-----------|------|----------|
| 主题 | `src/themes/<theme-key>/` | `rules/theme-guide.md` |
| 项目资料和文档 | `src/resources/` | `rules/resource-management-guide.md` |
| 项目术语 | `CONTEXT.md` | 本文档与相关规则中的统一词汇 |

## ⚠️ 重要原则

1. **需求与设计是继续工作的门禁**
   - 产品需求未确认时，禁止继续找视觉参考、拆页面结构、整理 `DESIGN.md` 候选、写设计方案或开发实现；先用简短摘要让用户确认目标用户、核心任务、范围/功能清单、内容来源和验收重点
   - 设计方案未确认时，禁止继续写规格/计划或开发实现；先确认设计基底、信息架构、交互路径、关键组件取舍和视觉方向
   - 发现目标、范围、内容来源、验收重点、信息架构、交互路径、视觉方向或设计基底存在多种合理选择时，立即停在对应门禁补充对齐

2. **设计要判断何时收敛、何时发散**
   - AI 应自行判断当前需要收拢需求还是探索解法：需求不清先收敛；需要改善体验或创新表达时再发散。发散是为了帮助用户选择最终方向
3. **业务原型按生产级界面处理**
   - 前端原型和后台原型默认都是可运行、接近正式产品的业务页面，不是黑白灰线框图或低保真草稿；只有用户明确要求时才使用低保真、wireframe、placeholder 等表达
4. **不要把截图当唯一真相**
   - 截图用于视觉参考；有代码、组件、设计系统、业务资料或用户说明时，要结合上下文判断
5. **早展示，早反馈**
   - 产品需求、设计方案或原型应尽早交给用户确认，不要等到全部完成后才暴露方向问题
   - 涉及页面意图、组件取舍或多方案比稿时，优先用低成本、快速的 Markdown ASCII Wireframe/Diagram 或 Mermaid 展示方案，先对齐需求
6. **讲人话，用户不懂技术**
   - 用用户能理解的方式说明取舍、风险和结果；用户无法执行 CLI 命令，不得省略验收流程
   - 向用户请求反馈或验收时，提醒用户尽量提供截图、预览链接、页面路径或具体问题位置，便于准确定位和复现
7. **业务原型与原型浏览工具必须隔离**
   - `src/` 只放业务/原型相关内容：前端原型、后台原型、主题、文档、内容配置、通用 UI 组件
   - `tools/` 只放原型浏览工具：路由编排、侧边栏、顶部工具栏、批注、快捷键、复制到 Figma、动态导入配置
   - 后台原型仍放在 `src/prototypes/`，不能因为页面是管理端、B 端或数据工作台就放入 `tools/`
   - 跨层引用必须通过 alias：`@/...` 指向 `src/`，`@tools/...` 指向 `tools/`
   - 新增功能前先判断属于哪一层；不确定时参考 `rules/architecture-guide.md`
8. **每次回复结尾都带上** `🐱喵`

## 项目结构

```text
├── src/                          # 内容层：只放业务/原型相关内容
│   ├── assets/                   # 静态资源
│   ├── common/                   # 内容层共享组件
│   ├── components/ui/            # 通用 UI 组件（Button、Input 等）
│   ├── content/                  # 项目内容配置
│   │   └── annotations/          # 批注默认分类与示例数据
│   ├── docs/                     # 项目文档与模板
│   ├── lib/                      # 通用工具函数
│   ├── prototypes/               # 前端原型和后台原型目录
│   ├── resources/                # 项目资料、文档和素材
│   ├── themes/                   # 主题与设计规范
│   └── workspace/                # 工作区内容页面（首页/主题/文档）
├── tools/                        # 原型浏览工具实现
│   ├── App.tsx                   # 主应用组件
│   ├── components/               # 工具组件（DocDrawer、AnnotationLayer 等）
│   ├── config/                   # 动态导入与配置
│   ├── hooks/                    # 工具层 Hooks
│   ├── lib/                      # 工具函数
│   ├── shell/                    # 界面外壳（Sidebar、Topbar）
│   └── tools/                    # 工具功能实现（批注、快捷键等）
└── rules/                        # Agent 工作规则
```
