# ChatRoom Page PRD

## 1. 页面概述

### 1.1 页面信息
| 项目 | 内容 |
|------|------|
| **页面名称** | ChatRoom Page |
| **页面路径** | /chat/:id |
| **页面类型** | 核心功能页 |
| **目标用户** | 已登录用户 |
| **页面目的** | 进行实时对话交流 |

### 1.2 页面描述
聊天室页面，提供实时消息收发功能，支持文字、图片、语音、视频等多种消息类型，显示对方在线状态和已读回执。

---

## 2. 展示逻辑

### 2.1 布局结构
```
┌─────────────────────────────────────┐
│ ← 👤 Amara Okafor    📞  📹  ⠇    │ ← Header
├─────────────────────────────────────┤
│                                     │
│         Yesterday, 10:30 AM         │ ← 时间分隔
│                                     │
│ ┌─────────┐                         │
│ │ Hey!    │  👤                    │ ← 接收消息
│ └─────────┘                         │
│                                     │
│                         ┌─────────┐ │
│                    👤   │ I'm good│ │ ← 发送消息
│                         └─────────┘ │
│                              ✓✓     │ ← 已读回执
│                                     │
│  ─────────────────────────────────  │
│  🎤 │ Message...           😊 │＋│  │ ← 输入栏
│  ─────────────────────────────────  │
└─────────────────────────────────────┘
```

### 2.2 消息气泡结构

#### 接收消息
```
┌─────────────────────────────────────┐
│                                     │
│  ┌──────────┐                       │
│  │ 消息内容  │  👤                  │
│  └──────────┘                       │
│            10:30 AM                 │
└─────────────────────────────────────┘
```

#### 发送消息
```
┌─────────────────────────────────────┐
│                                     │
│                       ┌──────────┐  │
│                  👤   │ 消息内容  │  │
│                       └──────────┘  │
│                 10:32 AM ✓✓         │
└─────────────────────────────────────┘
```

### 2.3 视觉设计

#### 颜色规范
| 元素 | 颜色值 | 变量名 |
|------|--------|--------|
| 页面背景 | #f5f3f3 | --surface-container-low |
| 接收气泡 | #ffffff | --surface-container-lowest |
| 发送气泡 | #ffffff | --surface-container-lowest |
| 发送文字 | #1a1a1a | --on-surface |
| 时间文字 | #9e9e9e | --on-surface-variant |
| 已读标记 | #944931 | --secondary |

---

## 3. 交互逻辑

### 3.1 消息操作

#### 发送消息
- 输入文字点击发送
- 回车键快速发送
- 支持换行（Shift+Enter）
- 回复模式下发送会引用原消息

#### 长按消息（消息操作菜单）
长按任意消息气泡（500ms以上）触发操作菜单，菜单显示在按压位置附近：

| 操作项 | 图标 | 功能描述 | 视觉样式 |
|--------|------|----------|----------|
| Reply | 弯曲箭头 | 进入回复模式，引用该消息 | 默认文字色 |
| Copy | 两个方块 | 复制消息文本到剪贴板 | 默认文字色 |
| Forward | 双箭头 | 转发消息到其他聊天（待实现） | 默认文字色 |
| Pin | 书签 | 置顶/取消置顶消息 | 默认文字色 |
| Select | 方框勾选 | 进入消息多选模式 | 默认文字色 |
| Delete | 垃圾桶 | 删除该消息 | 红色警示色 |

**菜单行为：**
- 菜单宽度：180px
- 菜单项高度：44px
- 圆角：12px
- 背景：白色 + 阴影
- 点击外部区域关闭菜单
- 菜单自动调整位置避免超出屏幕

#### 回复模式
- 长按消息选择"Reply"进入回复模式
- 输入栏上方显示回复预览条：
  - 左侧显示被回复消息的发送者和内容（截断显示）
  - 右侧显示取消按钮（X）
  - 预览条左侧有主题色边框装饰
- 发送的消息显示回复引用：
  - 消息气泡顶部显示灰色引用条
  - 格式："Replying to [Sender]: [Message text]"
- 点击取消按钮或发送消息后退出回复模式

#### 消息置顶
- 长按消息选择"Pin"置顶消息
- 置顶消息右上角显示 📌 图标
- 已置顶消息菜单显示"Unpin"选项，点击取消置顶
- 置顶状态持久化（当前会话）

#### 消息多选
- 长按消息选择"Select"进入多选模式
- 进入多选模式后：
  - 头部变为"X Selected"标题，左侧显示关闭按钮，右侧显示 Delete 按钮
  - 所有消息的多选框统一对齐到屏幕最左侧，形成一列垂直参考线；发送消息的气泡仍保持右对齐，接收消息的气泡保持左对齐
  - 底部固定显示 Forward 按钮，显示已选数量
- 点击 Forward 跳转至 /forward-message 转发已选消息
- 点击 Delete 打开二次确认对话框，确认后批量移除已选消息
- 点击关闭按钮或删除完成后退出多选模式

#### 消息删除
- 长按消息选择"Delete"打开删除确认对话框
- 对话框包含：
  - 标题："Delete Message"
  - 提示文字："Are you sure you want to delete this message?"
  - "Delete for everyone"复选框（仅对自己发送的消息显示）
  - Cancel按钮：取消删除，关闭对话框
  - Delete按钮：确认删除，从列表移除消息
- 删除后消息从列表中移除

#### 双击消息
- 快速进入回复模式（快捷操作）

### 3.2 聊天设置菜单（头部菜单）
点击右上角三点菜单按钮展开：

#### 免打扰
- 点击切换免打扰状态
- 图标变化：静音时显示带斜线的铃铛
- 文字变化："Mute Notifications" / "Unmute Notifications"

#### 清空聊天记录
- 点击打开确认对话框
- 对话框内容：
  - 标题："Clear History"
  - 提示："Are you sure you want to clear all messages in this chat? This action cannot be undone."
  - Cancel按钮：取消操作
  - Clear按钮：确认清空所有消息
- 清空后聊天保留在列表中

#### 删除聊天
- 点击打开确认对话框
- 对话框内容：
  - 标题："Delete Chat"
  - 提示："Are you sure you want to delete this chat? All messages will be removed."
  - Cancel按钮：取消操作
  - Delete按钮：确认删除
- 删除后返回聊天列表

#### 语音通话
- 点击发起语音通话
- 关闭菜单后执行

#### 视频通话
- 点击发起视频通话
- 关闭菜单后执行

### 3.3 附件发送

| 类型 | 操作 |
|------|------|
| 图片 | 点击相册图标选择 |
| 相机 | 点击相机图标拍照 |
| 语音 | 长按麦克风录制 |
| 文件 | 点击附件图标选择 |
| 位置 | 点击位置图标分享 |

### 3.3 语音消息

#### 录制
- 长按麦克风按钮
- 上滑取消发送
- 松开发送

#### 播放
- 点击播放/暂停
- 显示波形和时长
- 支持拖动进度

### 3.4 已读回执
| 状态 | 图标 |
|------|------|
| 发送中 | ⏳ |
| 已发送 | ✓ |
| 已送达 | ✓✓ |
| 已读 | ✓✓（蓝色） |

### 3.5 输入栏（微信风格）
固定于屏幕底部。布局：语音切换 | 文本输入框（内嵌表情按钮） | 加号/发送按钮

**组件行为：**
- **语音按钮**（左侧）：麦克风图标，点击进入语音录制模式
- **文本输入框**（中间）：白色圆角输入框（rounded-2xl），placeholder "Message..."，表情按钮位于输入框内右侧
- **加号/发送**（右侧）：
  - 无文字时：显示"+"图标，点击打开附件菜单底部弹窗，激活时旋转45°
  - 有文字时：显示发送箭头（primary 颜色，无圆形背景）

### 3.6 附件菜单（微信风格底部弹窗）
点击"+"按钮触发：
- 半透明黑色遮罩 + 底部白色面板（rounded-t-3xl）
- "Share"标题 + 关闭按钮 + 4列网格菜单
- 菜单项：Photos、Camera、Files、Location（统一样式，surface-container 灰色背景）
- 点击遮罩或关闭按钮关闭

---

## 4. 内容规范

### 4.1 时间分隔
| 时间范围 | 显示格式 |
|----------|----------|
| 今天 | Today |
| 昨天 | Yesterday |
| 本周 | Monday |
| 更早 | December 20, 2023 |

### 4.2 消息类型
| 类型 | 显示方式 |
|------|----------|
| 文字 | 纯文本 |
| 图片 | 缩略图，点击查看大图 |
| 视频 | 封面+播放按钮 |
| 语音 | 波形+时长+播放按钮 |
| 文件 | 图标+文件名+大小 |
| 位置 | 地图缩略图+地址 |

---

## 5. 异常处理

### 5.1 网络异常
- 发送失败：显示重试按钮
- 接收失败：自动重连

### 5.2 空状态
- 无消息：显示"No messages yet"

---

## 6. 主题适配

使用 Equatorial Minimalism 主题，发送消息使用主色背景。
